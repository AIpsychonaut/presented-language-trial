Standard agent evaluations publish a named game G in methods text while a harness executes a wired predicate R. We define presentation as binding of string G at locus π_k, and presentation without wiring as G remaining in presented text L after the lab has left R as the only payoff. A G-labeled integer in that state is a type error; extra work of zero still leaves the integer untyped. A weaker checker remains legal after G is stripped from L.

Public math and code items carry an answer box or a unit-test suite as R, with a frozen named method or spec clause as G. The trial writes G into the tool or file locus on one arm (inject) and omits it on the other, then lists named-constraint invocation after R already passed. If L presents G on an active locus, wire G or strip G. Official W is a named lemma after a correct answer. Presented-but-unwired G can move work toward G while R stays flat.
